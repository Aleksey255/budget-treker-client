// import { useApi } from "@/hooks/useApi";
// import type { Transactions } from "@/types/transaction";

export const Transaction = () => {
  // const { data: transactions, addItem: addTransaction } = useApi<Transactions>('/transactions');
  return <div></div>
}
